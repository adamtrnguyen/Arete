# Load CLAUDE.md from additional directories added via --add-dir
export CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1

# Persist Claude Code tasks across sessions for this project
export CLAUDE_CODE_TASK_LIST_ID=arete
