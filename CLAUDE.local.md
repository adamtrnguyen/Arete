# Arete — Local Context

## Current State
- Main development on `main` branch
- Obsidian plugin and Python CLI in same repo
- Optional deps (agent, mcp) not installed in base env — pyright errors on those are expected

## Active Work
- NAS Archive reorganization ongoing — file paths will change
